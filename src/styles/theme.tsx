import { createTheme } from '@mui/material';

const loginTheme = createTheme({
  palette: {
    background: {
      default: '#363740',
      paper: 'white',
    },
    primary: {
      main: '#3751FF',
      100: '#9FA2B4',
    },
  },
});

export default loginTheme;
