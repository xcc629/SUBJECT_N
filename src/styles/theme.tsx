import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    background: {
      default: '#363740',
      paper: 'white',
    },
    primary: {
      main: '#3751FF',
      100: '#A4A6B3',
      200: '#9FA2B4',
      300: '#DDE2FF',
      400: '#525468',
    },
  },
});

export default theme;
